const PAGE_ID = 'flexpix';

document.addEventListener('DOMContentLoaded', () => {
  console.log(`${PAGE_ID} loaded`);

  // future hooks:
  // - add/remove picks
  // - flex payout logic
  // - projection locking
});