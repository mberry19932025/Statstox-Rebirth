const PAGE_ID = 'deposit-withdraw';

document.addEventListener('DOMContentLoaded', () => {
  console.log(`Loaded ${PAGE_ID} page`);

  // Frontend-only for now
  // Backend will later:
  // - gate VIP access
  // - fetch live contest states
});
